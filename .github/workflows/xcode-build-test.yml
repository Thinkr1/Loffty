name: Xcode - Build and Test

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-15]
    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ matrix.os }}-deriveddata-${{ hashFiles('**/*.pbxproj', '**/Package.resolved') }}
          restore-keys: |
            ${{ matrix.os }}-deriveddata-

      - name: Cache SPM packages
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/org.swift.swiftpm
          key: ${{ matrix.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ matrix.os }}-spm-

      - name: Build & Test
        run: |
          set -o pipefail
          mkdir -p TestResults
          xcodebuild test \
            -project Loffty.xcodeproj \
            -scheme Loffty \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
            -resultBundlePath TestResults/Loffty.xcresult \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="-" \
            COMPILER_INDEX_STORE_ENABLE=NO \
            SWIFT_ACTIVE_COMPILATION_CONDITIONS="DEBUG CI"

      - name: Test summary
        if: always()
        run: |
          xcrun xcresulttool get test-results summary \
            --path TestResults/Loffty.xcresult || true

      - name: Convert xcresult to JUnit
        if: always()
        run: |
          brew install xcresultparser
          xcresultparser -o junit TestResults/Loffty.xcresult \
            > TestResults/junit.xml

      - name: Upload xcresult
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcresult-${{ matrix.os }}
          path: TestResults/Loffty.xcresult
          if-no-files-found: ignore

      - name: Upload JUnit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-${{ matrix.os }}
          path: TestResults/junit.xml
          if-no-files-found: ignore

      - name: Publish test report
        if: always()
        uses: dorny/test-reporter@1cc81a5edf733718d4850df304aaa21c05cd7280 # v3
        with:
          name: LofftyTests (${{ matrix.os }})
          path: TestResults/junit.xml
          reporter: java-junit
          fail-on-error: false
